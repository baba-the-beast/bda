# Pinned for reproducible builds. Versions verified together against the test
# suite; every pin publishes a cp311 manylinux wheel, matching the CI runner.
fastapi==0.141.1
uvicorn==0.53.0
pydantic[email]==2.13.5
email-validator==2.3.0
python-multipart==0.0.32
pymongo==4.18.1
prometheus_client==0.26.0
