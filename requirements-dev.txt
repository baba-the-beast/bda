# Development and CI tooling. Never installed into a production image:
# render.yaml and the service Dockerfiles install requirements.txt only.
-r requirements.txt
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1
ruff==0.16.7
mypy==2.3.1
